{
  "fields": [
    {
      "type": "description",
      "key": "zendeskConnectionDescription",
      "content": "Enter your Zendesk subdomain (the part before .zendesk.com in your Zendesk URL), then authorize the connection below."
    },
    {
      "type": "text",
      "label": "Zendesk Subdomain",
      "key": "zendesk_subdomain",
      "required": true,
      "placeholder": "your-company"
    }
  ]
}
