{
  "api_name": "update_ticket",
  "name": "Update Ticket",
  "plugin_description": "Integrates with Zendesk to manage support tickets from Kizen agentic workflows.",
  "action_description": "Changes fields on an existing ticket: status, priority, type, assignee, group, tags, or custom fields. Only the fields you set are changed.",
  "runtime": "python-3-13",
  "action_type": "zendesk_update_ticket",
  "script": "script.py",
  "secrets": [
    "zendesk_subdomain"
  ],
  "inputs": [
    {
      "name": "ticket_id",
      "label": "Ticket ID",
      "data_type": "string",
      "required": true,
      "input_source": "variable"
    },
    {
      "name": "ticket_status",
      "label": "Status",
      "hint": "One of: new, open, pending, hold, solved, closed.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "priority",
      "label": "Priority",
      "hint": "One of: urgent, high, normal, low.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "type",
      "label": "Ticket Type",
      "hint": "One of: problem, incident, question, task.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "assignee_id",
      "label": "Assignee ID",
      "hint": "Numeric Zendesk agent ID — visible in the agent's profile URL in Zendesk Admin Center (Team members).",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "group_id",
      "label": "Group ID",
      "hint": "Numeric Zendesk group ID — visible in the group's URL under Admin Center → People → Groups.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "add_tags",
      "label": "Add Tags",
      "hint": "Comma-separated list of tags to add, e.g. vip,billing. Existing tags not listed are left untouched. Zendesk does not allow adding tags on a closed ticket.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "remove_tags",
      "label": "Remove Tags",
      "hint": "Comma-separated list of tags to remove, e.g. vip,billing. Tags not listed are left untouched. Zendesk does not allow removing tags on a closed ticket.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "custom_fields",
      "label": "Custom Fields",
      "hint": "JSON array string of custom field objects, e.g. [{\"id\": 360000123456, \"value\": \"abc\"}]",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    }
  ],
  "outputs": [
    {
      "name": "ticket_id",
      "label": "Ticket ID",
      "data_type": "string",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "ticket_status",
      "label": "Status",
      "data_type": "string",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "updated_at",
      "label": "Updated At",
      "hint": "ISO 8601 timestamp as returned by Zendesk.",
      "data_type": "datetime",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    }
  ]
}
