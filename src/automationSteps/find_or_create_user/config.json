{
  "api_name": "find_or_create_user",
  "name": "Find or Create User",
  "plugin_description": "Integrates with Zendesk to manage support tickets from Kizen agentic workflows.",
  "action_description": "Looks up a Zendesk end user by Zendesk User ID, External ID, email, or name (in that priority order), optionally creating them if no match exists.",
  "runtime": "python-3-13",
  "action_type": "zendesk_find_or_create_user",
  "script": "script.py",
  "secrets": [
    "zendesk_subdomain"
  ],
  "inputs": [
    {
      "name": "zendesk_user_id",
      "label": "Zendesk User ID",
      "hint": "Zendesk's own internal numeric user ID (not External ID — see that field for the distinction). Highest-priority search key — a direct, unambiguous record fetch, checked before External ID, Email, or Name.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "external_id",
      "label": "External ID",
      "hint": "e.g. a Kizen contact ID, stored on the Zendesk user record for cross-referencing. Used to find an existing user (via Zendesk's guaranteed-unique external ID lookup) if Zendesk User ID isn't set, before Email or Name are considered. Also stored on the record when creating a new user.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "user_email",
      "label": "Email",
      "hint": "Used to find an existing user if neither Zendesk User ID nor External ID is set, or create one if Create If Missing is enabled.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "user_name",
      "label": "Name",
      "hint": "Lowest-priority search key — only used to find a user if none of Zendesk User ID, External ID, or Email is set. Required if Create If Missing is true and no user matches yet.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "phone",
      "label": "Phone",
      "hint": "Only used when creating a new user.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "organization_id",
      "label": "Organization ID",
      "hint": "Numeric Zendesk organization ID — visible in the organization's URL under Admin Center → People → Organizations. Only used when creating a new user.",
      "data_type": "string",
      "required": false,
      "input_source": "variable"
    },
    {
      "name": "create_if_missing",
      "label": "Create If Missing",
      "hint": "If true and no user matches, creates one. Defaults to false — no user is created otherwise.",
      "data_type": "boolean",
      "required": false,
      "input_source": "variable"
    }
  ],
  "outputs": [
    {
      "name": "user_id",
      "label": "User ID",
      "data_type": "string",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "user_name",
      "label": "Name",
      "data_type": "string",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "user_email",
      "label": "Email",
      "data_type": "string",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "organization_id",
      "label": "Organization ID",
      "data_type": "string",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "role",
      "label": "Role",
      "hint": "One of: end-user, agent, admin.",
      "data_type": "string",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    },
    {
      "name": "was_created",
      "label": "Was Created",
      "hint": "True if a new user was created; false if an existing user was found.",
      "data_type": "boolean",
      "conflict_resolution": "overwrite",
      "create_field_options": false
    }
  ]
}
